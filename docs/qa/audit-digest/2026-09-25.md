# External audit — 2026-09-25 (daily digest findings)

source: ArchitectIT daily digest feed, generated 2026-09-25T09:14:32Z
repos_covered: 1 blocks

## aiggp-agentic-framework
- summary: Four workstreams overnight: size-gate shell scope plus forced structural splits, runner-owned image cycling with two false-green fixes, fleet secret-scanning sprints from gate to hub reporting, and the identity re-pin operation audited into nine defects.
- finding: The size gate did not measure shell scripts; its first catch after the fix was the nearly six-hundred-line enroll script it had been failing to police for an unknown period.
- finding: The image cycler could reap the pinned image and still report converged, and the CI publish job shipped artifacts without human request; both false-green sources fixed with ledger records explaining why.
- finding: PyYAML module-scope import took down the test suite in two separate suites before the dependency-free workflow reader landed; recurrence proves the first fix was local, not systemic.
- finding: Podman's local digest was read as the registry digest in CI and the hard gate carried the same wrong-axis read, so the identity pin needed two corrections; the re-pin suite also silently borrowed the operator's git identity until CI caught it.
- finding: The mutation harness had drifted into not executing; consolidation made one harness with a self-check that it actually runs.
- next: Add a gate banning module-scope optional-dependency imports in test files, and audit what other file classes the size gate remains colorblind to (SQL, generated code, config).
