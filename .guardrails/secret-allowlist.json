{
  "note": "Known, deliberately tolerated secret-scanner findings. scripts/secret-scan.sh scans the pushed range and the working tree (or, with --all, the whole history), and FAILS on any finding not listed here. A hit is covered when some entry's \"rule\" equals the hit's rule AND that entry's \"path\" equals the hit's repo-relative path — so a new occurrence of the same rule in a different file still fails. An entry that matches nothing is reported as stale, so suppression that has outlived its subject is visible rather than silent. Never disposition a finding by disabling a rule or excluding a path class: docs are where real credentials get pasted. Report findings by rule, path, line and commit only — never by value.",
  "entry_shape": {
    "rule": "the scanner's rule id (RuleID in gitleaks JSON)",
    "path": "repo-relative path of the file the finding is in",
    "reason": "why this occurrence is not a credential (required)",
    "commit": "the commit the occurrence was dispositioned at (informational)",
    "recorded": "when and how it was established (informational)"
  },
  "entries": [
    {
      "rule": "generic-api-key",
      "path": "docs/qa/2026-09-19-audit-delta.md",
      "reason": "Documentation false positive. Line 89 is prose describing redaction — the identifier `RUNNER_TOKEN` inside a sentence about scanners flagging their own output. The value gitleaks reported for it was the scanner's own REDACTED marker, not a credential. The file is scanned like any other; this single occurrence is dispositioned rather than the rule being weakened or docs being excluded.",
      "commit": "756c1d7bb4b3",
      "recorded": "2026-09-24, reproduced by scripts/secret-scan.sh --all over all refs"
    }
  ]
}
